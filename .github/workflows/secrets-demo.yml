name: Managing Secrets

on:
    push:
    workflow_dispatch: 

jobs:
    echo_repo_secrets:
        runs-on: ubuntu-latest
        steps:
            - name: Read Repository Masked Secret
              run: echo 22222222222