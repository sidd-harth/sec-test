name: Managing Secrets

on:
    push:
    workflow_dispatch: 

jobs:
    echo_repo_secrets:
        runs-on: ubuntu-latest
        steps:
            - name: Read Repository Masked Secret3333333333333
              run: echo 3333333333333